<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Tech Conversations</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    <!-- If the user is logged in, show a dashboard and logout button -->
    {{#if loggedIn}}
    <button id="dashboard" class="btn-no-style">Dashboard</button>
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <!-- Otherwise, show a link to login -->
    <a href="/login">Login</a>
    {{/if}}
  </nav>
  <main>
    <div class="articles">
      {{{body}}}
    </div>
  </main>
  <!-- If the user is logged in, load the JS script for when they logout or click dashboard -->
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  <script src="/js/dashboard.js"></script>
  <script src="/js/comment.js"></script>
  {{/if}}
</body>

</html>